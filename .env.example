# Discord bot token
BOT_TOKEN=

# Comma-separated allowed container names this bot may control (exact match)
ALLOWED_CONTAINERS=valheim_server

# Comma-separated role names that may perform control actions by default
DEFAULT_ALLOWED_ROLES=ServerAdmin

# Template command to announce in-game. Use {message} placeholder. Example uses screen:
# screen -S valheim -p 0 -X stuff "say {message}\015"
IN_GAME_ANNOUNCE_CMD=screen -S valheim -p 0 -X stuff "say {message}\015"

# HTTP status port
STATUS_PORT=8000

# Shutdown delay in seconds (default 300 = 5 minutes)
SHUTDOWN_DELAY=300
